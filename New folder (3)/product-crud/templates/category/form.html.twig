{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<div class="row mb-4">
    <div class="col">
        <h1>{{ title }}</h1>
    </div>
    <div class="col text-end">
        <a href="{{ path('category_index') }}" class="btn btn-secondary">&larr; Annuleren</a>
    </div>
</div>

<div class="card">
    <div class="card-body">
        {{ form(form) }}
    </div>
</div>
{% endblock %}
